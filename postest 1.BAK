
predicates
nondeterm meminjam_buku(symbol,symbol)
	  membeli_buku(symbol,symbol)
	  membaca_buku(symbol,symbol)
	  melihat_buku(symbol,symbol)
	
	
	
clauses
meminjam_buku(cli,ikii).
meminjam_buku(ani,bebet).

membeli_buku(eka,tini).
membeli_buku(selli,mar).

membaca_buku(ani,bebet).
membaca_buku(wiji,rina).

melihat_buku(fitri,selli).
melihat_buku(dinda,elis).

goal
melihat_buku(Who,elis).
